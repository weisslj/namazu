##
AUTOMAKE_OPTIONS = 1.4 no-dependencies

schemedir = $(pkgdatadir)/scheme

scheme_DATA = http.pl

EXTRA_DIST = $(scheme_DATA)

ETAGS_ARGS = $(scheme_DATA) --lang=perl
TAGS_DEPENDENCIES = $(scheme_DATA)
