$Id: TODO-satoru,v 1.14 2000-01-20 11:38:25 satoru Exp $

やるべきことの備忘録 by 高林

  * mknmz.pl.in: File::Copy を使おう

  * rcfile.c: syntax error のメッセージを gettext化

  * rcfile.c: エラーメッセージに directive名を入れる?

  * nmz_free_idxnames(), nmz_free_aliases(), nmz_free_hlist()
    nmz_free_replaces() をまとめて行う関数が欲しい。

  * 一対多の alias に対応する。例: Alias all-ml foo bar baz quux
    - 今の struct nmz_indices の構造では難しい

  * idxnames.c: インデックスの数の制限を廃止する (リスト構造?)
    - struct nmz_indices の構造を見直す

  * nmz/libnamazu.c を廃止 (中の関数は適切なファイルに移動する)

  * nmz/libnamazu.h -> nmz/libnmz.h

  * extern 関数を吟味する -> できるだけ減らす (static にする)

  * hlist と名のつく関数の名前を変える

  * NmzResult(旧HLIST) 関連の処理 -> 戻り値をポインタでもらう
    NULLポインタでエラーを返す

  * var.[ch] の扱いはどうすべきか (廃止したいが NMZ は残したい)

  * 文書整備


将来の構想

  * 3.0 は一から書き直したい、という野望はあるけれど…
    - まだまだプログラミングについて勉強不足
    - 書き直している時間が取れるか疑問
